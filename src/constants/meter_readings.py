# this is the value that your reads when connected to the UI on the controller and you send the voltage from the test DC power supply
meter_reading = [0.1, 0.2, 0.4, 0.7, 1.0, 2.0, 3.0, 4.0, 6.0, 8.0, 10.0]
